import { getDayInput } from '../support/getDayInput';

const dummyInput = ``;

describe('Day dayNo - dayTheme', () => {
  describe('Part 1 - part1Theme', () => {
    it.skip('should pass a dummy test', () => {
      const input = dummyInput;
      expect(input).toEqual('');
    });

    it.skip('should pass the real test', () => {
      const input = getDayInput('dayNo');
      expect(input).toEqual(0);
    });
  });
});
